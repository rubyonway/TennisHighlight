---
name: tennis-video-review
description: 从用户拍摄的网球训练或比赛视频制作完整回合集锦、逐球技术统计、带证据等级的落点示意和可播放视频的统一 HTML 复盘报告。适用于“剪网球高光”“统计正反手”“估计落点”“分析动作”“把已有网球视频和数据整合成 H5”，也支持从已有文件恢复未完成的网球复盘。不是专业鹰眼判罚、雷达测速或通用生成视频质量评测工具。
---

# 网球视频复盘

把视频、逐球记录和技术建议放到一个可回看的交付入口。统计能追溯到原片时间，建议能看到对应动作，估计值与可见事实分开呈现。

本 skill 提供分析与交付流程、数据口径、统计校验脚本和本地视频预览服务。它不是已经训练好的网球检测模型，也不保证任意机位都能自动识别全部击球。根据素材选择人工复核、已有检测工具或两者结合。

## 何时使用

- 给定网球原片，剪辑指定球员的精彩集锦，并保留完整回合。
- 按全片统计正手、反手、回球结果，回看具体技术问题。
- 在单机位视频中辅助展示可见球路、落点或有依据的粗估。
- 已有集锦、JSON、图片或文字报告，整理成统一 HTML，避免重复分析。
- 网球任务中断后，从现有文件恢复工作。

若只问通用网球知识，直接回答；若只是排查应用连接错误，使用当前环境的应用诊断能力。不要把这些请求扩展为视频项目。

## 先确定范围

从用户消息和现有项目中确认下面信息；能读取到的不要重复提问。

| 输入 | 处理原则 |
| --- | --- |
| 目标球员 | 用衣着、近远场和身份提示定位。镜头或场边交换后重新核对，不默认近端始终是本人。 |
| 视频或已有项目路径 | 检查实际存在、时长、尺寸、帧率和音轨。外接盘离线时先找缓存，标明缓存画质、帧率、声音及是否完整。 |
| 请求范围 | 区分剪辑、全片统计、落点修订、动作建议、报告整合，按用户要求组合，不强制每次全做。 |
| 剪辑偏好 | 时长、横竖屏、现场音、轨迹、顺序、是否完整回合。没有规定时，以连续回合和原声为默认。 |
| 估计许可 | 请求精确指标时先说明可见证据能支持什么；用户接受“大概”时可估计，但仍保留依据、假设和缺失值。 |
| 交付位置 | 优先现有项目与当前环境的输出规则，一个项目一个报告入口，不硬编码个人目录或历史存储路径。 |

## 路由与执行

### 1. 恢复或整合已有结果

先读当前目录的项目说明、最近交付、时间轴和 JSON。核对哪个版本已经完成，哪些只是计划或未验证的中间产物。原任务连接中断不意味着文件消失。

用户只要求整合报告时，复用已完成视频与数据，不重新跑检测，不重新估算指标。若现有材料相互冲突，选择有可追溯证据的最新版本，并在必要处保留未确定项。

目录合并属于用户明确要求的整理动作，不是恢复任务的必选步骤。需要合并时先列文件和依赖，保留可恢复的历史材料。大量脚本仍依赖旧绝对路径时，可留下兼容链接；交付媒体必须在移动整包后仍可访问，不能只依赖旧目录或外接盘。

### 2. 分析原片、剪集锦或修订落点

读取 [视频分析与剪辑](references/video-analysis.md)。先用一个完整回合跑通识别、标注、渲染和播放，再扩展；包含 FFmpeg 的并发作业不超过 4 个。

- 回合保留到死球或明确结束，不能在活球中间切掉结尾。
- 动态构图随球路与击球阶段调整，保留双方与潜在落地区域。不要用整段统一放大替代跟随构图。
- 全片统计覆盖声明的原片范围；高光片段只支持高光样本结论。
- 将击球类型、回球结果、落点依据分开标注。类型不明的球仍可有已知结果，反之亦然。
- 看见对方继续击球只支持“回合延续／估计有效”，不能直接认定落点界内。
- 短暂的离拍轨迹可能支持很多不同落点。不能为了让图好看，把场外点直接截到场内。
- 用户提供“多数在底线内”等观察，可作为显式先验；“多数”不能自动当成每个可疑球均在界内。缺少逐球证据时给区域或留空，不强制把所有点拟合进场内。
- 不可靠速度可删去。可解释的粗估必须带样本量、时间区间、空间假设与名称，不冒充离拍初速或三维挥拍峰值。

### 3. 计算统计与技术建议

按 [数据与报告约定](references/data-and-report.md) 保留逐球 ID、原片、时间、类型、结果和依据。可用下面脚本重新计算汇总；它只校验和汇总已有标注，不执行视频识别，也不能证明标注正确。

```bash
python3 scripts/summarize_events.py /path/to/events.json --output /path/to/summary.json
```

脚本采用明确的事件口径，失败会报错而不是悄悄丢弃不合格记录。未知结果不填成功或失败，零个可判结果的有效率为 `null`。每项比例同时显示分子、分母与未知数量。

动作建议按“可见现象 → 可能需要改进的动作 → 一句提醒 → 可执行练法”表达，附原片时间和画面。把因果推断与观察分开。训练目标是建议值，不混进本次实测指标；不要从几帧影像作伤病诊断或精确技术定级。

### 4. 生成一个统一报告

读取 [数据与报告约定](references/data-and-report.md) 的报告与交付部分。默认浅色、桌面宽屏、固定目录、编号分节。将概览、播放器、技术统计、落点、动作建议和逐球明细放进一个 HTML 入口；按本次有据可用的内容增减。

本地交付默认 HTML 加相对路径媒体目录，CSS、JS 和小型 JSON 可内联，大视频不转 base64。已有获授权的稳定媒体 URL 可以继续用。不得因生成报告而自动上传私人视频、复用历史公司 OSS 或发布到云端。

本地预览可使用仅依赖 Python 标准库的 Range 服务：

```bash
python3 scripts/serve_report.py /path/to/report-folder --port 8921
```

脚本只监听 `127.0.0.1`，到输出地址打开报告；停止当前进程即可关闭预览。端口被占用时换端口，不结束无关进程。

## 验收与交付

按实际修改范围验证：

1. 汇总能从逐球记录重算，未知值保留，原片与集锦时间轴不混用。
2. 新生成／重编码的视频检查音视频解码、时长和开头／中间／结尾；只改报告时无需重新编码视频。
3. 实际在浏览器播放；验证章节、逐球时间与落点跳转，筛选、空结果与分页，目录导航和窄窗口布局。HTTP `206` 只证明支持 Range，不能替代实际播放检查。
4. 落点图从空场按弹地时间累计；跳转前后与视频时间一致。若只提供静态总览，明确其覆盖范围，不伪装实时识别。
5. 局部修订核对非目标内容。目录迁移核对文件清单，已有视频可用字节校验或同 inode 验证未改动。
6. 提供唯一报告入口、必要媒体目录及确实存在的限制。未通过的验证明确写出，不能把已生成当作已验证。

## 调用示例

- “用 $tennis-video-review 剪这三段视频，保留完整回合，统计我全片的正反手表现。”
- “用 $tennis-video-review 复核黄点偏深问题，只修有证据的落点，其余视频保持不变。”
- “用 $tennis-video-review 把已有集锦、逐球 JSON 和动作建议整合成一个可播放的 H5，不重新识别。”

不要把历史案例的球数、时长、颜色、身高、原片数量或目录结构当作新任务的事实。
